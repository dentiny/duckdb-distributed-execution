# name: test/sql/setup_distributed_server.test
# group: [sql]

# Setup file for distributed execution tests
# This starts the Flight server in a background thread
# Include this at the beginning of tests that need distributed execution

require duckherder

# Start the distributed server on port 8815
statement ok
SELECT duckherder_start_local_server(8815);
