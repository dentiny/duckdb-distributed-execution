# name: test/sql/version.test
# description: test duckherder extension version
# group: [sql]

require duckherder

query T
SELECT extension_version
FROM duckdb_extensions()
WHERE extension_name = 'duckherder';
----
0.0.9
